<Window x:Class="SPJ_APP.View.SyncProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="🔄 Sinkronisasi Data Cloud Supabase" Height="480" Width="620"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#F8FAFC"
        Loaded="Window_Loaded" Closing="Window_Closing">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Panel -->
        <Border Grid.Row="0" Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="12" Padding="16" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal">
                <Border Background="#EEF2FF" CornerRadius="10" Width="44" Height="44" Margin="0,0,14,0" VerticalAlignment="Center">
                    <TextBlock Text="⚡" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Proses Sinkronisasi Data Real-Time" FontSize="16" FontWeight="Bold" Foreground="#0F172A"/>
                    <TextBlock Text="Sedang menyelaraskan database SQLite lokal dengan Cloud Supabase..." FontSize="12" Foreground="#64748B" Margin="0,2,0,0"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Active Progress Bar & Status Title -->
        <StackPanel Grid.Row="1" Margin="0,0,0,14">
            <DockPanel Margin="0,0,0,6">
                <TextBlock x:Name="TeksStatusSaatIni" Text="Memulai koneksi ke server..." FontSize="12.5" FontWeight="SemiBold" Foreground="#4F46E5"/>
            </DockPanel>
            <ProgressBar x:Name="ProgressBarSync" Height="8" IsIndeterminate="True" Foreground="#4F46E5" Background="#E2E8F0"/>
        </StackPanel>

        <!-- Detailed Terminal / Console Log Box -->
        <Border Grid.Row="2" Background="#0F172A" BorderBrush="#334155" BorderThickness="1" CornerRadius="10" Padding="12" Margin="0,0,0,16">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Text="💻 DETAIL LOG PROSES SINKRONISASI:" Foreground="#94A3B8" FontSize="10.5" FontWeight="Bold" Margin="0,0,0,8"/>
                <TextBox x:Name="TxtLogDetails" 
                         Background="Transparent" 
                         Foreground="#38BDF8" 
                         BorderThickness="0" 
                         FontFamily="Consolas, Courier New, monospace" 
                         FontSize="11.5" 
                         IsReadOnly="True" 
                         TextWrapping="Wrap" 
                         VerticalScrollBarVisibility="Auto"/>
            </DockPanel>
        </Border>

        <!-- Bottom Action Button -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="TombolSelesai" 
                    Content="✅ Selesai &amp; Tutup" 
                    IsEnabled="False" 
                    Height="36" 
                    Padding="20,0" 
                    Background="#4F46E5" 
                    Foreground="White" 
                    FontWeight="Bold" 
                    Click="TombolSelesai_Click"/>
        </StackPanel>
    </Grid>
</Window>
