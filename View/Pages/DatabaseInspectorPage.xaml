<UserControl x:Class="SPJ_APP.View.Pages.DatabaseInspectorPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="LightYellow" BorderBrush="Orange" BorderThickness="1" Padding="10" Margin="0,0,0,10">
            <TextBlock Text="PERINGATAN: Alat ini memodifikasi database lokal secara langsung dan melewati semua validasi bisnis (misal: stok, total nota tidak akan dihitung ulang). Gunakan hanya untuk perbaikan darurat. Risiko data korup ditanggung pengguna." TextWrapping="Wrap" FontWeight="Bold" Foreground="Red"/>
        </Border>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Pilih Tabel:" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox x:Name="ComboTables" Width="200" SelectionChanged="ComboTables_SelectionChanged"/>
        </StackPanel>

        <DataGrid x:Name="DataGridContent" Grid.Row="2" AutoGenerateColumns="True" CanUserAddRows="False"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="BtnSaveChanges" Content="Simpan Perubahan" Padding="10,5" Click="BtnSaveChanges_Click"/>
            <Button x:Name="BtnDeleteSelected" Content="Hapus Baris Terpilih" Padding="10,5" Margin="10,0,0,0" Click="BtnDeleteSelected_Click"/>
        </StackPanel>
    </Grid>
</UserControl>