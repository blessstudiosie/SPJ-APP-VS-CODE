<UserControl x:Class="SPJ_APP.View.Pages.SalesPersonListPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Master Data Sales" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="TombolTambah" Content="Tambah Sales" Padding="10,5" Margin="10,0,0,0" Click="TombolTambah_Click"/>
            <TextBox x:Name="TeksStatus" VerticalAlignment="Center" Margin="15,0,0,0" FontStyle="Italic"
                     IsReadOnly="True" BorderThickness="0" Background="Transparent" IsReadOnlyCaretVisible="True"/>
        </StackPanel>

        <DataGrid x:Name="TabelSales" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" AlternatingRowBackground="#F5F5F5"
                  MouseDoubleClick="TabelSales_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nama" Binding="{Binding Name}" Width="180"/>
                <DataGridTextColumn Header="Telepon" Binding="{Binding Phone}" Width="130"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="180"/>
                <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="100"/>
                <DataGridTextColumn Header="Target Omset" Binding="{Binding TargetOmset, StringFormat={}{0:N0}}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock x:Name="TeksJumlah" Grid.Row="3" Margin="0,10,0,0" FontWeight="Bold"/>
    </Grid>
</UserControl>